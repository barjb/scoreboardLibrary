# AGENTS.md — scoreboardLibrary

## Build tooling

- Adding dependencies and plugins to `pom.xml` is the first step for any real task.
- Standard Maven directory layout: `src/main/java/`, `src/main/resources/`, `src/test/java/`.

